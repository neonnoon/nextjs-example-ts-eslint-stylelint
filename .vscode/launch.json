{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run",
            "command": "npm run dev",
            "request": "launch",
            "type": "node-terminal",
            "envFile": "${workspaceFolder}/.env.local"
        }
    ]
}
